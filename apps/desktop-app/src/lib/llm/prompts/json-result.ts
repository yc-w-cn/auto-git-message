
export const PROMPT_JSON_RESULT = `请按照以下格式返回数据，不要包含多余的解释文字，summary应当简洁，description应当具体，他们内容并不相同，只返回JSON格式的数据：

{
  "summary": "chore(log): add log function",
  "description": "1. add logic \\n2. add unit test",
}`